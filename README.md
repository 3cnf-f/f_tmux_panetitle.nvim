# f_tmux_panetitle.nvim
set the current pane title in tmux as the current buffer name if it ends with .py
otherwise it is set to NOT_PYTHON

in my case it is so that the filename can be read in another tmux pane using and used to run the current .py
